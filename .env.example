# Database Configuration
# Get your MongoDB URI from MongoDB Atlas (Cluster > Connect > Drivers)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/portfolio?retryWrites=true&w=majority

# Optional: Site URL (Used for sitemap and metadata if you re-add it)
# NEXT_PUBLIC_SITE_URL=https://yourportfolio.com
